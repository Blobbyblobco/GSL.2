{
  "name": "gold-stake-lotto",
  "private": true,
  "type": "module",
  "description": "Gold Stake Lotto website: static site on Netlify with a small settings API and admin page.",
  "scripts": {
    "dev": "npx netlify-cli@latest dev",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^11.1.1"
  },
  "engines": {
    "node": ">=20"
  }
}
