{
  "name": "gold-stake-lotto",
  "private": true,
  "type": "module",
  "description": "Gold Stake Lotto website: static site on Vercel with a small settings/forms API and admin page.",
  "scripts": {
    "dev": "npx vercel@latest dev",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": "22.x"
  }
}
